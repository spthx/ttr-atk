# タタルの大繁盛店
## 買収戦アップデート指示書

対象プロジェクト: `tataru-world-trade-ledger`  
目的: 買収戦を「数値を監視する画面」から、FF14風に状況を確認しながらコマンドを選ぶバトルへ改善する。

---

# 1. 今回の更新方針

今回の更新では、物件・都市・敵AIの種類を増やさない。

重点は次の5点。

1. 買収戦の開始前に、対戦相手と発動効果を確認できるようにする。
2. バトル中の細かい内部数値を減らし、キャラクター・所有率・資金の山を見やすくする。
3. 資金源・かけひき選択中は、FF7Rのコマンド選択に近いスローモーションにする。
4. LIMIT BREAKを企業網の成長によって強化される、一交渉一回の超必殺技にする。
5. SHORT、決着、OVERKILLをプレイヤーが確認してからリザルトへ進むようにする。

既存の資金計算、所有率ゲージ、独立危険度、企業離反、敵予算の基本ロジックはできる限り維持する。

---

# 2. 用語と表示テキスト

## 2.1 オープニング2枚目

現在の見出しを変更する。

変更前:

> 金貨を積み、所有率を奪え。

変更後:

> **ギルを積み、魂を奪え。**

本文も現在のFINAL PUSH限定説明に寄せすぎず、次の内容へ変更する。

> 自社と傘下企業のギルを積み、競合の防衛資金と所有率を押し切れば買収成立です。

対象:

- `src/components/LaunchIntro.tsx`
- 必要に応じて `app/layout.tsx` のdescription
- READMEおよびゲーム内ヘルプ

---

## 2.2 風・戦況効果

自社有利時の主表示:

> **自社資金効果上昇**

演出:

- 緑色の風または魔法の光が、自軍側へ向かって流れる。
- テロップは短くてよい。
- 所有率ゲージ内の小型表示は残してよい。
- `BURST`は補助的な英字表示として使用してよいが、日本語の主表示は上記で統一する。

敵有利時の主表示:

> **敵大規模防衛出資**

演出:

- 赤い警告色。
- FF14の敵詠唱バーや強攻撃予告に近い見せ方。
- 予告後、敵コインがまとまって積み上がる。
- `ONSLAUGHT`は補助英字として残してよいが、日本語の主表示は上記で統一する。

---

## 2.3 SHORT

敵の追加防衛資金が0になった状態は、`BREAK`ではなく`SHORT`と呼ぶ。

表示:

```text
SHORT!

敵の防衛資金が枯渇！
最後の一手で決着をつけろ
```

表示中はバトルを停止する。

ボタン:

> 続行する

`BREAK`という語は、将来「敵がすでに積んだ資金や防衛体制を妨害技で崩した状態」に使えるよう温存する。

---

# 3. 買収戦開始前ブリーフィング

現在の自動消滅テロップを、確認ボタン付きの開始前画面へ変更する。

## 3.1 表示内容

```text
BUYOUT OPERATION

自社カンパニー名
VS
買収対象企業名

対象都市・業界
現在相場
仲介手数料

ACTIVE SYNERGY
発動中の効果一覧

ACTIVE ALLIANCE
同盟先と今回使用できる支援
```

ボタン:

> 効果を確認して交渉開始

このボタンを押すまで、以下は進行させない。

- 所有率ゲージ
- 敵AI
- COMMAND READY
- スキル再使用時間
- 風の残り時間
- 敵大規模防衛出資
- 毎秒のバトル内演出

開始画面は自動で閉じない。

## 3.2 SYNERGYの表示

今回の戦闘に実際に影響するものだけを表示する。

例:

```text
ACTIVE SYNERGY

鉱工業ネットワーク II
自社押込 +5%
敵防衛予算 -5%

黒衣森素材流通網
グループ資金要求を使用可能
毎秒収益倍率 x1.20
```

表示対象:

- 現在の`industryInfluence`
- 発動済み`GroupSynergy`
- `groupKeys`から成立しているグループ資金源
- 交易網補正

内部データを一度に全面統合する必要はない。  
今回の実装では既存計算を残し、UIと説明上で`SYNERGY`へまとめる。

---

# 4. SYNERGYの扱い

## 4.1 定義

SYNERGYは、自社傘下企業同士の業界・事業上のつながり。

含めるもの:

- 業界影響力
- 業界掌握
- 産業グループシナジー
- 原料・加工・輸送・販売の組み合わせ
- グループ資金要求

## 4.2 UI名称

以下のような内部名称は残してよいが、プレイヤー向け表示はSYNERGYへ統一する。

```text
SYNERGY I
SYNERGY II
SYNERGY III
SYNERGY COMPLETE
```

## 4.3 役割

SYNERGYは基本的に常時発動する。

主な効果:

- 自社の所有率押込補正
- 敵防衛予算の低下
- グループ資金要求の解放・強化
- 毎秒収益倍率
- 将来的にはLB投入額の補正

SYNERGY自体を、買収戦中に押す独立した新ボタンにはしない。

---

# 5. ALLIANCEの扱い

## 5.1 定義

ALLIANCEは、自社傘下ではない外部企業・組織との協力関係。

例:

- ガーロンド・アイアンワークス
- 地域の商会
- ギルド
- グランドカンパニー
- 港湾組織
- 地元有力者

SYNERGYが自社内部の連携であるのに対し、ALLIANCEは外部との協力。

## 5.2 現在の同盟機能

現在の同盟システムを基本として利用する。

- 一交渉につき1回のALLIANCE支援
- 同盟先の関連物件を攻撃すると同盟破棄
- 買収戦開始前に、同盟先と使用可能な支援額を表示
- ALLIANCE支援はLIMIT BREAKとは別コマンド

## 5.3 LIMIT BREAKとの分離

ALLIANCEは以下に数えない。

- LB解放に必要な企業数
- LB1・LB2・LB3の段階判定

ALLIANCE支援額も、当面はLBの基準額へ混ぜない。

理由:

- LBは自社と傘下企業による全社一斉出資
- ALLIANCEは外部から受ける一回限りの支援

この二つを分けることで、役割が明確になる。

---

# 6. LIMIT BREAK

## 6.1 基本方針

LIMIT BREAKは、一交渉につき一度だけ使用できる超必殺技。

- 企業網を育てるほど強くなる。
- 大きく所有率を押し込む。
- 敵をSHORTへ追い込める。
- 勝利ラインまで押し切った場合は、FINAL PUSHなしで直接勝利できる。
- どの敵でも必ず一撃で倒す即死技にはしない。
- 発動時に傘下企業の独立・離反リスクがある。

## 6.2 解放条件と段階

自社を1社として数える。

| 自社を含む企業数 | 段階 | 使用可否 |
|---:|---|---|
| 1～3社 | 未解放 | 使用不可 |
| 4～7社 | LIMIT BREAK I | 使用可能 |
| 8～15社 | LIMIT BREAK II | 強化 |
| 16社以上 | LIMIT BREAK III | 最大強化 |

ALLIANCEは企業数に含めない。

## 6.3 発動回数

```text
1交渉につき1回
```

専用stateを追加する。

例:

```ts
const [limitBreakUsed, setLimitBreakUsed] = useState(false);
```

使用後は、その買収戦が終わるまで再使用不可。

## 6.4 投入額

現在の全社投入を基礎にする。

```text
LB基準額
= 自社LB枠
+ 独立しなかった全傘下企業のLB支援額合計

LB最終投入額
= LB基準額 × 段階倍率
```

自社LB枠:

```text
買収対象の現在相場 × 24%
```

傘下企業LB支援額:

```text
各社の現在相場 × 24%
```

段階倍率は調整可能な定数として一か所へまとめる。

初期値:

```ts
const LIMIT_BREAK_MULTIPLIERS = {
  1: 1.00,
  2: 1.15,
  3: 1.30,
} as const;
```

数値調整をしやすいよう、関数内へ直接散らさない。

## 6.5 独立・離反

現在の全社投入仕様を維持する。

- LB参加企業の独立危険度 +12
- 企業ごとに独立判定
- 独立した企業はLB支援額へ参加しない
- 過去にその企業が積んだ支援額も失う
- 戦闘終了後に傘下から離脱
- 現在の強制清算処理を維持

発生時の表示:

```text
DEFECT!

○○社が支援を撤回
```

複数社の場合は一覧表示してよい。

今回、複雑な新アニメーションは必須ではない。  
企業名、失われた支援額、残ったLB投入額が読めればよい。

## 6.6 LBによる直接勝利

LB適用後の所有率をクランプする前に計算する。

```text
LB適用後の生所有率 >= 100%
```

なら、そのまま勝利。

この場合:

- FINAL PUSHは発生させない。
- 決着方法を`LIMIT BREAK I/II/III`として保存。
- OVERKILLを保存。
- 勝利確認画面へ移る。

100%未満なら戦闘続行。

敵防衛資金が0になった場合はSHORT表示へ移る。

---

# 7. FINAL PUSHと通常決着

## 7.1 FINAL PUSH

敵がSHORTした後に自社資金を直接投入した場合、現在どおりFINAL PUSHを発生させる。

ただし、現在のように強制的に`-99 → -100`へ固定するだけでなく、実際の投入量から生所有率を計算する。

## 7.2 継続圧力による勝利

敵がSHORTした後、既存の資金差による継続的な押込だけで100%へ達した場合も勝利してよい。

この場合の決着方法例:

```text
CAPITAL PRESSURE
継続資金圧力
```

## 7.3 決着方法の記録

次のような型を追加する。

```ts
type FinishMethod =
  | 'LIMIT_BREAK_1'
  | 'LIMIT_BREAK_2'
  | 'LIMIT_BREAK_3'
  | 'FINAL_PUSH'
  | 'CAPITAL_PRESSURE'
  | 'NORMAL';
```

`onBattleEnd()`へ渡す結果にも含める。

---

# 8. OVERKILL

## 8.1 目的

勝利時に、必要な100%をどれだけ超えて押し切ったか表示する。

LBだけでなく、通常のFINAL PUSHや継続圧力にも適用する。

## 8.2 計算

現在の画面表示は0～100%へクランプするが、決着判定時だけ生値を保存する。

```text
生所有率 = (100 - 生ゲージ値) / 2

OVERKILL = max(0, 生所有率 - 100)
```

例:

```text
生所有率 138.4%
OVERKILL +38.4%
```

## 8.3 表示

勝利確認画面またはリザルト:

```text
BUYOUT COMPLETE

FINISH
LIMIT BREAK II

FINAL OWNERSHIP
138.4%

OVERKILL
+38.4%
```

OVERKILLが0に近い場合:

```text
JUST FINISH
100.4%
```

評価文は初期実装では簡単でよい。

例:

| OVERKILL | 表示 |
|---:|---|
| 0～4.9% | 精密交渉 |
| 5～19.9% | 決定的優勢 |
| 20～49.9% | 圧倒的買収 |
| 50%以上 | 過剰制圧 |

この評価は資金効率の優劣ではなく、決着の派手さを示す。

---

# 9. バトル画面の情報整理

## 9.1 残す表示

- 自社と敵のキャラクター
- 自社名と敵企業名
- 所有率ゲージ
- コイン・資金の積み上がり
- 自社残り資金%
- 敵追加防衛資金%
- COMMAND READY
- 敵の次回投入ゲージ
- 自社資金・資金源・かけひきタブ
- SHORT
- LIMIT BREAK
- 風・バフ・敵大規模防衛出資

## 9.2 削除する表示

`capital-scoreboard`:

```text
自分の積み額 VS 敵の積み額
```

同じ金額がコイン領域にも表示されているため削除する。

## 9.3 通常画面から隠す表示

`battle-readouts`にある以下の項目は通常表示から削除する。

- 所有率速度
- 資金圧力
- 決着予測
- 資金源リスク
- 交易連携の細かな補正値
- 競合の次手の重複表示

内部計算は残す。

必要なら戦局ログまたはヘルプ内だけで確認可能にする。

## 9.4 空いた領域の利用

- キャラクターを少し拡大
- 企業名を読みやすくする
- 所有率ゲージを太くする
- コインの山を大きくする
- 自社・敵の残り資金%を視認しやすくする

---

# 10. コインの積み方

## 10.1 目標

現在の整然とした円盤タワーから、ロマサガ3のトレードを連想できる「不規則に積み上がる資金の山」へ変更する。

## 10.2 見た目

- 同じ高さの縦列を規則的に並べない。
- 山の中央を高くし、左右へ低く広がる。
- コインごとに少し左右位置、回転、幅を変える。
- 新しい資金投入時に、上から数枚が落ちて山へ加わる。
- 自社は金色、敵は赤・紫系。
- 金額の数字と残り資金%は山の手前または中央に残す。
- 札束・金塊のような厚みを感じる形でもよい。
- 外部画像素材は追加せず、CSSで表現してよい。

## 10.3 実装上の注意

React再描画ごとに完全な乱数を使わない。

`index`と`side`から決定的に位置を作る。

例:

```ts
const seed = side === 'player' ? index * 17 : index * 23;
const x = ((seed * 7) % 100) / 100;
const rotation = (seed % 9) - 4;
```

表示上限を設ける。

例:

```text
最大100～140枚
```

上限を超えた分は山の高さ・密度・金額表示で表現する。

---

# 11. コマンド選択中のスローモーション

## 11.1 条件

通常:

```text
自社資金タブ = 1.0倍
```

スローモーション:

```text
資金源タブ = 0.25倍
かけひきタブ = 0.25倍
```

## 11.2 遅くする対象

同じ`timeScale`を適用する。

- 所有率ゲージ
- 敵AIの投入準備
- COMMAND READY
- スキル再使用時間
- 風の残り時間
- 継続スキル時間
- 数字・コインの進行演出

CSSアニメーションだけを遅くするのではなく、ゲームロジックの経過時間へ適用する。

## 11.3 停止する場面

以下は`timeScale = 0`。

- 開始前ブリーフィング
- SHORT確認
- 勝利・敗北確認
- リザルト
- ヘルプを開いている間

戦局ログは0.25倍でもよいが、実装が簡単なら停止してよい。

---

# 12. テロップと確認待ち

## 12.1 LIMIT BREAK

現在より長く見せる。

流れ:

1. `LIMIT BREAK I/II/III`
2. 参加企業・離反企業を確定
3. 約1.5～2秒の溜め
4. 資金を一斉投入
5. 所有率、SHORT、勝利判定

テロップ表示中は通常進行を止める。

## 12.2 SHORT

敵防衛資金が0になった瞬間に一度だけ表示する。

自動で閉じない。

## 12.3 勝利・敗北

勝敗直後に自動で分析リザルトを出さない。

まず中間確認画面を出す。

勝利例:

```text
BUYOUT COMPLETE

○○社の買収が成立

FINISH
LIMIT BREAK II

OVERKILL
+38.4%

買収結果を確認する
```

敗北例:

```text
BUYOUT FAILED

競合に所有率を押し切られた

敗因を確認する
```

ボタンを押して既存のタタル分析画面へ進む。

---

# 13. iPhone Safariの横引っ張り

上下スクロールは維持する。

左右の無駄な引っ張りと横方向はみ出しを抑える。

`index.css`で確認・追加する。

```css
html,
body,
#root {
  width: 100%;
  max-width: 100%;
  overflow-x: hidden;
  overscroll-behavior-x: none;
}

body {
  position: relative;
}
```

必要ならモバイル時のみ:

```css
@media (max-width: 639px) {
  body {
    touch-action: pan-y;
  }
}
```

ただし、CSSで隠すだけでなく、以下の実際のはみ出しも調べる。

- `100vw`
- 固定幅
- `transform`
- `position:absolute`
- 画面外へ出るテロップ
- コインの山
- 長い企業名
- iframe内外の幅

上下スクロールとボタン操作を壊さないこと。

---


# 13.5 入力したカンパニー名の使用

買収戦内では、起動時にプレイヤーが入力した`companyName`を使用する。

現在の固定表示「タタル商会」は、可能な範囲で`companyName`へ置き換える。

主な表示箇所:

- 開始前ブリーフィングの自社名
- 所有率ゲージ左側の自社名
- SHORT後・勝利確認画面
- リザルトの自社表示
- 戦局ログ

実装:

- `App.tsx`から`BattleModal`へ`companyName`をpropsで渡す。
- 24文字の入力上限は現在のまま維持する。
- 狭いHUDでは1行省略表示を使用する。
- 開始前画面やリザルトでは最大2行まで折り返してよい。
- `title`または`aria-label`には完全な名称を残す。
- 固定幅を広げて画面全体を押し出さない。

推奨CSS:

```css
.company-name-compact {
  min-width: 0;
  max-width: 100%;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.company-name-full {
  overflow-wrap: anywhere;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
```

カンパニー名の採用そのものを理由に、キャラクター・ゲージ・コマンド領域を縮小しないこと。

---

# 13.6 空いた画面領域の再配分

表示を削除して空白が生まれても、単純に各コンテナを縦横へ引き伸ばさない。

再配分の優先順位:

1. 自社と敵のキャラクターを少し大きくする。
2. 所有率ゲージを太くし、自社名・敵企業名を読みやすくする。
3. コインの山を大きくし、資金量の差を見やすくする。
4. コマンドボタン同士の間隔を広げる。
5. 資金源・かけひきの説明文を読みやすくする。
6. 上下左右に適度な余白を設ける。

禁止:

- 空いた領域を意味のない大型パネルで埋める。
- ボタンを小さくして一列へ過密配置する。
- 文字サイズだけを大きくして説明が切れる状態にする。
- キャラクターを背景へ追いやる。
- 画面全体の高さを増やし、主要コマンドを画面外へ送る。

モバイルでは、資金源・かけひきの項目数が多い場合は、無理に一画面へ詰めず縦スクロールを使用してよい。

---

# 13.7 新システム追加に伴う敵の暫定強化

SYNERGY、ALLIANCE、段階式LIMIT BREAK、LB直接勝利が追加されるため、敵を暫定的に少し強化する。

複数のAIパラメータを同時に変更せず、敵総予算へ一つの補正を掛ける。

推奨:

```ts
const ENEMY_BALANCE_FACTOR = {
  tutorial: 1.00,
  normal: 1.10,
  cartelHQ: 1.15,
} as const;
```

敵総予算:

```text
既存の敵総予算 × 敵バランス補正
```

条件:

- 最初の初心者向け物件は強化しない。
- 通常物件は約10%強化。
- 企業連合本部は約15%強化。
- 初期投入42%、残り58%、AI投入間隔は今回変更しない。
- 数値は一か所の定数から後で調整できるようにする。
- プレイ不能になるほどの難易度変更は行わない。

今回の目的は、新システム分の余裕を少し相殺することであり、敵AIの全面強化ではない。

---

# 13.8 実装開始前の質問

実装前に既存コードと指示書を確認する。

仕様の衝突、必要データの不足、既存機能を壊す可能性など、合理的に判断できず実装を止める必要がある点だけ、最大3点まで質問してよい。

質問不要:

- 既存コードから判断できる点
- 後から調整可能な倍率・秒数
- 細かな装飾の好み
- CSSの内部実装方法

質問がなければ、確認待ちで止まらず、そのまま実装・型検査・ビルドまで進める。

質問する場合は、可能なら選択肢と推奨案を添える。


# 14. 主な変更対象ファイル

必須:

```text
src/components/BattleModal.tsx
src/components/LaunchIntro.tsx
src/App.tsx
src/types.ts
src/data/initialData.ts
src/data/helpText.ts
src/components/SkillsSynergyView.tsx
src/components/CartelAllianceView.tsx
src/index.css
src/battle-buyout.css
src/battle-balance.css
src/battle-clarity.css
src/battle-command-refine.css
src/battle-enemy-budget.css
src/battle-final-wind.css
README.md
```

必要に応じて:

```text
app/layout.tsx
src/components/WindIndicator.tsx
src/utils/formatter.ts
```

新しい外部ライブラリは追加しない。

---

# 15. 実装の推奨順序

Codexの残量を節約するため、この順番で作業する。

## 第1段階: 状態制御

1. `battlePhase`または同等の状態を追加
2. 開始前停止
3. SHORT停止
4. 勝敗確認停止
5. リザルトへの手動遷移
6. timeScale

推奨例:

```ts
type BattlePhase =
  | 'briefing'
  | 'active'
  | 'short_notice'
  | 'finisher_notice'
  | 'result';
```

## 第2段階: LIMIT BREAK

1. 4・8・16社判定
2. 一交渉一回
3. ALLIANCEをLBから分離
4. 段階倍率
5. 離反判定
6. LB直接勝利
7. OVERKILL

## 第3段階: UI整理

1. `capital-scoreboard`削除
2. `battle-readouts`削除または非表示
3. キャラ・所有率・コイン領域拡大
4. コインの山を不規則化
5. 風・敵大規模防衛出資の表示文言

## 第4段階: 文書と確認

1. LaunchIntro更新
2. ヘルプ更新
3. README更新
4. iPhone横幅確認
5. lint/build

---

# 16. 変更しないもの

今回、以下は作り直さない。

- 10都市の進行順
- 26物件の追加・削除
- 敵AIの戦略全面改修
- セーブ機能
- オンラインPvP
- 新しい画像素材の追加
- サーバー側経済処理
- 企業連合本部の基本攻略構造
- 独立後の強制清算
- 現在の仲介手数料と勝敗精算の基本構造

無関係なリファクタリングを行わない。

---

# 17. 完了条件

## 起動・開始

- 買収対象企業名が開始前画面に表示される。
- 発動中SYNERGYが確認できる。
- ALLIANCE支援の有無が確認できる。
- 開始ボタンを押すまで一切進行しない。

## バトル

- 資金源・かけひき中は0.25倍になる。
- 自社資金へ戻ると1.0倍になる。
- COMMAND READYは残っている。
- コインと残り資金%が見える。
- 細かな速度・圧力表示は通常画面にない。
- 重複した積み額VS表示がない。

## LIMIT BREAK

- 4社未満では使えない。
- 4・8・16社で段階が変わる。
- 一交渉で一度しか使えない。
- ALLIANCEは企業数へ含まれない。
- 独立判定が機能する。
- 100%を超えればFINAL PUSHなしで勝利する。
- 100%未満なら戦闘が続く。

## SHORT・決着

- SHORTが一度だけ表示される。
- 自動で閉じない。
- 勝敗直後にリザルトへ飛ばない。
- 決着方法とOVERKILLを確認できる。
- ボタンでタタル分析へ進む。

## モバイル

- iPhone Safariで横へページ全体を引っ張りにくい。
- 上下スクロールは維持される。
- 360px前後でも企業名、コイン、コマンドが切れない。
- 下端のOS UIとボタンが重ならない。

---

# 18. Codexへ渡す実行命令

```text
添付の「買収戦アップデート指示書」を正として実装してください。

実装開始前に既存コードと指示書を確認してください。
合理的に判断できず実装を止める必要がある重大な不明点だけ、最大3点まで質問してください。
質問がなければ確認待ちで停止せず、そのまま実装・型検査・ビルドまで進めてください。

重要:
- READMEと現在の実装を確認してから変更する
- 新しい外部ライブラリを追加しない
- BattleModal.tsxを全面的に別設計へ書き換えない
- 無関係なリファクタリングをしない
- public/gameの生成済みJS/CSSを直接編集しない
- distを直接編集しない
- デプロイ、commit、pushは行わない

作業後:
1. pnpm run lint
2. pnpm run build:game
3. 問題がなければ pnpm run build
4. READMEを現在仕様へ更新
5. 変更ファイル、実装内容、確認結果を簡潔に報告

ビルド失敗時だけ、今回の変更に直接関係するエラーを修正して再実行してください。
追加提案や次期機能は実装しないでください。
```
